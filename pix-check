#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"

printf 'Validation de pix-ssg %s...\n\n' "$(cat VERSION)"
./vendor/bin/pest
npm test
npm run lint
npm run format:check
./pix-build

printf '\nTous les contrôles sont réussis.\n'